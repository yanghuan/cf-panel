# cf-panel 全项目代码 Review

- **审查时间**：2026-08-04 18:24:41 CST
- **审查范围**：Worker 后端（`src/index.js`）、D1 Schema、Durable Objects、前端（`public/`）、Rust Agent（`agent/rust/`）、systemd 服务、CI/CD、测试、部署配置与项目文档
- **审查方式**：全仓库静态审查、跨模块协议核对、现有测试执行、依赖漏洞审计
- **风险口径**：Critical = 可直接导致系统接管或核心安全边界失效；High = 高概率导致数据丢失、权限扩大、资源耗尽或生产核心能力失效；Medium = 需要特定条件、影响受限或主要影响可靠性；Low = 防御纵深、可维护性、体验问题

## 1. 结论摘要

项目整体结构清晰，Worker、Durable Object、D1、静态前端和 Rust Agent 已形成完整闭环；现有 Node 测试覆盖面明显好于同规模项目，鉴权、PAT、MCP、DO 会话、告警和归档均有基础测试。

但当前仍不建议在公网无额外防护的情况下直接承载高权限生产主机，主要原因是：

1. `JWT_SECRET` 缺失时会回退公开固定值 `dev-secret`，属于 fail-open，可导致完整管理员身份伪造。
2. MetricsDO alarm 被持续上报不断后推，归档、数据清理和离线告警在正常运行状态下可能永不执行。
3. 归档流程先启动删除 DO Storage、后写 D1；D1 失败时可能永久丢失唯一副本。
4. 终端/文件会话无配额，终端重试在 Agent 侧非幂等，可批量创建 PTY/bash 并耗尽主机资源。
5. 文件上传无背压且直接覆盖最终文件，断线、解码失败或超时会留下截断/半写文件。
6. systemd 默认以 root 启动带远程 shell 和任意文件读写能力的 Agent，凭据泄露或控制面失陷时影响为整机接管。

### 问题统计

| 严重性 | 数量 |
| --- | ---: |
| Critical | 1 |
| High | 11 |
| Medium | 18 |
| Low | 5 |

> 统计按根因合并；一个根因可能同时影响 Worker、前端和 Agent。

## 2. 架构与数据流概览

### 2.1 Worker 后端

- `src/index.js` 同时实现 REST API、JWT/PAT 鉴权、MCP、监控查询以及三个 Durable Object。
- `TerminalDO` 按 4 个固定分片中转 Agent 控制通道、终端流和文件流。
- `PanelDO` 负责面板服务器列表推送。
- `MetricsDO` 单实例保存分钟级热数据、执行告警、归档与保留期清理。
- D1 保存服务器、PAT、审计、历史监控、自定义指标和面板设置。

### 2.2 前端

- 无构建静态 SPA，使用原生 JavaScript、Chart.js 和 xterm.js。
- JWT/PAT 保存在 `localStorage`，REST 通过 Bearer 头鉴权，WebSocket 在首帧发送 token。
- 终端直接通过 xterm.js 与 TerminalDO 透传；文件管理使用 JSON + base64 分块协议。

### 2.3 Rust Agent

- 单进程 Tokio Agent，维持控制 WebSocket，按指令创建终端或文件数据通道。
- 监控读取 `/proc`、调用 `df`/`uname`/`hostname`，支持 TCP/HTTP 探活和自定义命令指标。
- systemd 服务默认拥有完整主机权限，并提供 PTY shell 与文件读写。

## 3. Critical

### C-01 缺少 `JWT_SECRET` 时使用公开固定密钥，生产环境可被直接接管

- **位置**：`src/index.js:83-85`、`src/index.js:108-125`、`src/index.js:184-186`；`wrangler.toml:41-44`
- **现象**：`secret(env)` 在未配置 `JWT_SECRET` 时返回固定字符串 `dev-secret`。JWT 签名和未单独配置 `HASH_SECRET` 时的 Agent/PAT 哈希都依赖该值。
- **攻击场景**：部署者配置了 `PANEL_PASSWORD`，但遗漏 `JWT_SECRET`；攻击者无需知道登录密码即可离线生成 `role:1` 管理员 JWT，调用终端和文件接口。README 虽提示必配，但运行时没有拒绝启动或拒绝登录。
- **影响**：面板管理员权限、所有已接入主机的远程 shell/文件访问能力完全失守。
- **修复建议**：
  1. 生产请求路径发现 `JWT_SECRET` 为空时直接返回明确的 503 配置错误，禁止登录和受保护接口。
  2. 删除生产代码中的固定回退；本地测试通过显式 `.dev.vars` 提供开发密钥。
  3. 强制最小长度并支持密钥版本/轮换；`HASH_SECRET` 与 JWT 密钥必须独立。
  4. 将当前“回退固定密钥”的单元测试改为“缺密钥拒绝服务”。

## 4. High

### H-01 MetricsDO alarm 会被持续上报无限后推

- **位置**：`src/index.js:1430-1442`、`src/index.js:1526-1531`、`src/index.js:1648-1700`
- **现象**：每次 `/report` 都把唯一 alarm 重设为 `Date.now() + 10 分钟`。Agent 正常每 3 秒或 120 秒上报，均小于 10 分钟。
- **影响**：只要任意 Agent 持续在线，归档、热区清理、30 天指标清理、90 天审计清理、离线/恢复告警都可能永不执行。所有服务器共用一个 `MetricsDO("main")`，问题具有全局性。
- **修复建议**：读取 `getAlarm()`，仅在没有 alarm 或新时间更早时设置；alarm 结束后固定重排，并在 `finally` 中保证异常时仍会注册下一次 alarm。补充“高频上报不推迟已有 alarm”的测试。

### H-02 归档在 D1 成功前启动删除 DO Storage，失败时永久丢数据

- **位置**：`src/index.js:1655-1680`
- **现象**：循环中执行 `this.state.storage.delete()` 并保存已启动的 Promise，之后才执行 `env.DB.batch(stmts)`。
- **影响**：D1 batch 因配额、批次过大或临时故障失败时，Storage 删除仍可能完成，数据在两端都不存在。
- **修复建议**：先收集语句和 key，按有界批次写 D1；每批确认成功后再删除对应 Storage key。保留 `INSERT OR IGNORE` 以支持幂等重试。

### H-03 热区/D1 查询硬切换形成确定的数据空洞

- **位置**：`src/index.js:17-20`、`src/index.js:224-241`、`src/index.js:1654-1680`；`README.md:322-325`
- **现象**：≤12h 只查 DO，>12h 只查 D1；但 alarm 把超过 60 分钟的数据从 DO 删除。
- **影响**：
  - 12h 查询正常仅剩约 1h；
  - 3d/7d/30d 查询缺少尚未归档的最近约 1h；
  - `ARCHIVE_TO_D1=0` 时文档声称保留 12h，代码实际上约 1h 后删除。
- **修复建议**：所有范围按时间边界合并 D1 和热区，以 `(server_id, ts)` 去重后再降采样；或至少将热区删除阈值恢复为 720 分钟，并修正文档与测试。

### H-04 终端/文件会话无配额，可耗尽 Agent 和 DO 资源

- **位置**：`src/index.js:625-662`、`src/index.js:979-1011`、`src/index.js:1122-1147`；`agent/rust/src/main.rs:144-185,262`
- **现象**：API、TerminalDO 和 Agent 都没有每用户、每服务器或全局并发上限。只要 Agent 数据流一端保持连接，会话不会按 10 分钟 TTL 回收。
- **攻击/失败场景**：持有合法 exec 权限者循环调用 `/api/terminal`，不连接浏览器；Agent 仍创建 bash/PTY 并连接数据 WS，会话可长期存活。
- **影响**：耗尽进程、PTY、FD、线程、WebSocket、DO 内存/Storage。
- **修复建议**：API/DO 与 Agent 双层配额；Agent 使用 `Semaphore`；创建后 15–30 秒无浏览器鉴权则回收；增加绝对最长会话时长和 429/503 测试。

### H-05 `open_terminal` 重试在 Rust Agent 侧非幂等，会重复创建 shell

- **位置**：`src/index.js:1005-1010,1156-1172`；`agent/rust/src/main.rs:144-171`；`agent/rust/src/session.rs:37-52,85-87`
- **现象**：DO 在未收到 `terminal_ready` 时重发相同 SID；Agent 每次都重新 spawn PTY/bash，并用同一 SID 覆盖 Map 中旧项，却不停止旧进程。
- **影响**：网络延迟即可产生多个孤儿 shell；旧任务结束时还可能删除新会话 Map 项，造成进一步状态错乱。
- **修复建议**：Agent 对 SID 建立 `starting/active` 幂等状态；重复请求只重发 ready。任务结束时仅删除仍指向自己的 Map 项；DO 挂接新数据流时拒绝或关闭同 SID 旧流。

### H-06 文件上传无背压，500MB 文件可形成巨大内存/网络队列

- **位置**：`public/app.js:590-621`；`agent/rust/src/session.rs:187-199,319-365`
- **现象**：前端不等待 `write_result` 就连续读取、base64 编码并发送所有块，也不检查 `WebSocket.bufferedAmount`。Agent 串行写盘。
- **影响**：500MB 文件可产生约 667MB base64 数据及额外复制，慢盘/NFS 下浏览器、DO 和网络队列持续膨胀，导致标签页或会话崩溃。
- **修复建议**：采用 stop-and-wait 或有限滑动窗口；ACK 必须包含真实写入字节数和期望下一 offset；设置 `bufferedAmount` 高低水位；优先使用二进制帧。

### H-07 文件上传直接截断最终文件，失败会破坏原文件

- **位置**：`agent/rust/src/session.rs:319-365`
- **现象**：offset 0 时直接对最终路径 `set_len(0)`；解码、写盘、连接或超时失败后会留下截断/半写文件。超时仅停止等待 `spawn_blocking`，不能取消已运行的阻塞写任务。
- **影响**：覆盖配置、数据库或业务文件时可造成不可恢复损坏；超时重试还可能与后台旧写入并发。
- **修复建议**：使用同目录临时文件和 `begin/write/commit/abort` 协议；严格校验 offset、总大小和可选哈希；`flush + sync_all` 后原子 rename；断线清理临时文件。

### H-08 空 PAT 白名单在列表接口中被解释为“全部服务器”

- **位置**：`src/index.js:400-414,433-466,688-705`
- **现象**：创建 `server_ids: []` 后保存 `[]`；`queryServersForUser` 仅在数组长度非零时过滤，因此空数组返回全量服务器。单服务器接口的 `canAccessServer` 却拒绝所有服务器，语义不一致。
- **影响**：本意为“无服务器”的 PAT 可通过 `/api/servers`、PanelDO 和 MCP 枚举全量服务器及状态/系统信息。
- **修复建议**：仅 `NULL` 表示全部，空数组必须返回空集；创建时规范化并对 NULL、空数组、非法数组分别测试。

### H-09 登录端点无应用内限流，默认 `workers.dev` 暴露时可暴力破解

- **位置**：`src/index.js:488-500`；`README.md:235-236`
- **现象**：代码明确将防暴力破解完全交给 Cloudflare Access，但 README 只是“推荐”，部署步骤仍直接引导访问 `workers.dev`。
- **影响**：未配置 Access、WAF 或 Rate Limiting 的常见部署可被无限尝试面板密码；所有用户仅凭密码登录且拥有管理员权限。
- **修复建议**：应用层按 IP + 账户做 DO/Rate Limiter 限制和指数退避；部署文档将 Access 从“推荐”改为生产必需并给出验证步骤；增加失败阈值测试。

### H-10 Agent 默认以 root 运行，远程终端和文件管理等价于整机 root

- **位置**：`agent/cf-panel-agent.service:18-38`；`agent/rust/src/main.rs:144-185`
- **现象**：service 未设置 `User=`/`Group=`，systemd 默认 root。虽然配置了部分 sandbox，但为文件管理保留了广泛文件系统访问，终端直接启动交互 shell。
- **影响**：Agent key、管理员 JWT、Worker 或依赖链任一失陷都可直接获得主机 root 权限。
- **修复建议**：默认创建专用低权限用户；将“只监控”“受限文件”“root 终端”拆成显式权限档位；生产默认 `DISABLE_EXEC=1`，启用执行必须有醒目风险确认；进一步限制 capabilities、可写路径和命令范围。

### H-11 删除服务器与并发上报存在竞态，可重新生成孤儿指标

- **位置**：`src/index.js:346-395,588-622`
- **现象**：上报先读服务器，再更新/写 MetricsDO；并发删除可在清理后继续完成旧上报，重新写入热区或自定义指标。Schema 没有外键约束。
- **影响**：被删除服务器的数据可能被重新归档；ID 重用时可能污染新服务器历史。
- **修复建议**：服务器先原子标记 `deleting`，上报条件更新并在写 DO 前复核；断开 Agent 后二次清理。Schema 增加外键/不可复用稳定标识，避免仅以自增 ID 关联历史。

## 5. Medium

### M-01 `server:exec` PAT 可创建会话，但 WebSocket 首帧只接受 JWT

- **位置**：`src/index.js:625-662,1229-1237`
- **现象**：REST 使用 `authUserByToken/canExec` 接受 PAT；TerminalDO 首帧却调用 `verifyJwt`，PAT 必然以 1008 断开。
- **影响**：公开支持的 PAT exec 功能不可用，API 契约自相矛盾。
- **修复建议**：WS 使用 `authUserByToken` 并重新校验会话服务器的 `canExec`；或明确取消 PAT 的交互执行能力。

### M-02 任意 WebSocket 断开会取消整个分片的终端确认重试

- **位置**：`src/index.js:1339-1374`
- **现象**：`cleanup` 无论关闭的是浏览器、文件流还是某台 Agent，都无条件清空全部 `pendingTerm`。
- **影响**：一个无关连接关闭即可让其他服务器丢失终端重试。
- **修复建议**：pending 记录保存 SID/server/agent 归属，只清理关联项；增加跨 SID、跨服务器隔离测试。

### M-03 Agent 流尚未挂接时，浏览器终端输入被静默丢弃

- **位置**：`src/index.js:1298-1323`；`test/e2e.sh:192-199`
- **现象**：浏览器鉴权后立即输入，而 `sess.agentWs` 尚为空时没有缓存或错误。E2E 通过重复发送命令规避竞态。
- **修复建议**：增加有上限的 user→agent 缓冲，挂接后按序刷新；前端在收到 ready 前禁用输入或显示连接状态。

### M-04 离线创建失败的会话会写入内存/Storage，且不一定安排回收 alarm

- **位置**：`src/index.js:979-1011,1122-1147`
- **现象**：先保存 session，后检查 Agent 是否在线；返回 502 时未回滚。新会话也未统一安排 TTL alarm。
- **修复建议**：先确认 Agent，后持久化；所有失败路径回滚；每次创建都安排清理 alarm。

### M-05 Storage 扫描和 D1 batch 均无分页/批次上限

- **位置**：`src/index.js:1403-1419,1478-1503,1655-1680`
- **现象**：`storage.list({prefix})` 一次物化全部键；所有归档语句一次提交。
- **影响**：alarm 积压或服务器增多后，单次执行可能超过内存、CPU、D1 batch 或调用时长限制。
- **修复建议**：固定 limit + cursor 分页；每批完成“D1 成功→Storage 删除”；为单次 alarm 设置最大处理量并续排近时 alarm。

### M-06 历史归档永久丢弃 `mem_total`

- **位置**：`schema.sql:43-53`；`src/index.js:1430-1439,1665-1674`
- **现象**：热区保存 `mem_total`，`metrics_min` 没有该列，归档和历史查询也不返回。
- **影响**：历史数据无法按当时总内存计算百分比，扩容后统计失真。
- **修复建议**：增加 D1 迁移列并同步归档/查询。

### M-07 告警冷却与探活状态仅在内存，重启后重复或漏发

- **位置**：`src/index.js:1382-1393,1533-1612`
- **现象**：`alertLast`、`probeState` 在 DO 重启时清空；离线状态则持久化，行为不一致。
- **影响**：重启后重复阈值告警、重复 probe_down，或漏掉 probe_recovered。
- **修复建议**：将关键去重状态持久化到 DO Storage，并设置合理过期时间。

### M-08 Webhook 失败被吞掉，且先更新去重状态

- **位置**：`src/index.js:321-340,1541-1545,1586-1612,1621-1644`
- **现象**：不检查 HTTP 状态，网络异常被吞；告警状态在发送成功前已经更新。
- **影响**：发送失败后仍进入冷却或离线已通知状态，关键告警不会重试。
- **修复建议**：`sendWebhook` 返回成功状态并检查 `response.ok`；发送成功后再提交去重状态，重要告警使用持久化 outbox。

### M-09 `sanitizeAlerts` 接受 PUT，但发送函数会静默改成 POST

- **位置**：`src/index.js:149-178,321-339`
- **现象**：配置允许 GET/POST/PUT，发送时仅 GET 保留，其余都使用 POST。
- **修复建议**：统一允许列表和执行逻辑，并为三种方法增加测试。

### M-10 大文件下载在浏览器中多次复制整文件，峰值内存可超过 1.5GB

- **位置**：`public/app.js:549-587`
- **现象**：保存所有二进制字符串分块，完成后 `join`，再复制到完整 `Uint8Array`，最后构造 Blob。
- **影响**：400–500MB 文件很容易使低内存浏览器卡死或崩溃。
- **修复建议**：流式写 File System Access API/StreamSaver；至少每块立即转 `Uint8Array` 并直接 `new Blob(parts)`，同时降低非流式上限。

### M-11 下载期间文件缩短时可能无限读取 `got=0`

- **位置**：`public/app.js:558-587`；`agent/rust/src/session.rs:272-309`
- **现象**：前端以初始 size 为终止条件；文件中途缩短时 Agent 可持续返回成功且 `got=0`，前端不断请求相同 offset。
- **修复建议**：`got===0` 且未完成时中止；响应携带当前文件 size/版本，客户端校验 offset 单调增长。

### M-12 Agent 接受明文 `ws://` 配置，Agent key 可被窃听

- **位置**：`agent/rust/src/main.rs:37-49,88-96,252-260`
- **现象**：帮助文档推荐 `wss://`，运行时未验证 scheme；key 作为 `X-Agent-Key` 发送。
- **影响**：误配置明文连接时，网络中间人可获取长期 Agent 凭据并接管终端/文件通道。
- **修复建议**：默认仅允许 `wss://`；本地开发需显式 `ALLOW_INSECURE_WS=1` 且只允许 loopback。

### M-13 Rust Agent 没有测试，正式 Release 仅要求编译成功

- **位置**：`.github/workflows/agent-rust.yml:6-36,74-82`；`.github/workflows/test.yml:51-56`；`agent/rust/src/`
- **现象**：未发现 Rust `#[test]`；每次相关 push 都直接构建并发布正式 Release，发布 workflow 不依赖 CI/E2E 成功。
- **影响**：可编译但终端幂等、文件原子性或重连行为错误的版本仍会自动成为正式产物。
- **修复建议**：只对受保护 tag 或成功 CI 的 `workflow_run` 发布；加入 `cargo test`、`fmt --check`、`clippy -D warnings` 和 Rust Agent 协议 E2E。

### M-14 Rust 可执行项目未提交 `Cargo.lock`

- **位置**：`agent/rust/Cargo.toml:14-21`；仓库不存在 `agent/rust/Cargo.lock`
- **影响**：依赖和浮动 `stable` 工具链使同一提交在不同日期解析出不同构建，降低可复现性并放大供应链风险。
- **修复建议**：提交 lockfile，CI 使用 `--locked`，固定 Rust 工具链版本，并增加 `cargo audit`/`cargo deny`。

### M-15 D1 缺少版本化迁移，README 的旧库升级流程不完整

- **位置**：`schema.sql:7-72`；`README.md:60-78`；`wrangler.toml:28-34`
- **现象**：`CREATE TABLE IF NOT EXISTS` 不会给旧表补列；Wrangler migrations 仅用于 DO class，不是 D1 schema。README 增量命令未覆盖所有现有列，单独 DROP `servers` 还会遗留历史表。
- **影响**：干净库测试通过，但旧生产库升级后 API 才报 SQL 错误；ID 重用可能串历史。
- **修复建议**：采用 `wrangler d1 migrations` 维护版本 SQL，并在 CI 执行旧 schema→迁移→数据保留测试。

### M-16 当前开发依赖存在已知高危公告

- **位置**：`package.json:14-16`、`package-lock.json`
- **验证结果**：`npm audit` 报告 3 项：1 high、2 moderate。核心为 `undici 7.0.0–7.28.0`，经 `miniflare`/`wrangler` 引入，包括信息泄露、解析崩溃和请求解析差异类公告。
- **影响**：主要影响本地开发、测试和 CI，不直接进入 Worker 运行时，但 CI 处理不可信响应时仍有风险。
- **修复建议**：升级到包含 `undici >=7.29.0` 的兼容 Wrangler 版本并重新审计；不要按 audit 建议盲目降级到旧 Wrangler。

### M-17 CI/Release 供应链固定不足

- **位置**：`.github/workflows/test.yml:18-20,30-44,51-53`；`.github/workflows/agent-rust.yml:13-36,74-80`
- **现象**：Actions 使用可变 tag；E2E 直接下载并执行 websocat，无 checksum；release job 全程持有 `contents: write`；Release 无校验和/SBOM/attestation。
- **修复建议**：Actions 固定 commit SHA；校验下载 SHA-256；最小化 job 权限；发布 checksum、SBOM 和 artifact attestation。

### M-18 前端将服务器公网 IP 发给第三方地理服务

- **位置**：`public/app.js:185-227`
- **现象**：每个浏览器会把 Agent 上报的公网 IP 查询到 `ipapi.co`，失败后再发送到 `ipwho.is`。
- **影响**：泄露被监控基础设施 IP 与管理员访问时序；也增加第三方可用性和 CSP 依赖。
- **修复建议**：默认关闭或明确提示；改为服务端可控代理/离线数据库；提供隐私开关和文档说明。

## 6. Low

### L-01 浏览器 token 存在 `localStorage`，且项目未设置 CSP 等安全响应头

- **位置**：`public/app.js:6,74-86`；`src/index.js:74-78`；`public/index.html`
- **说明**：当前审查未发现可直接利用的 DOM XSS，动态 HTML 也普遍调用 `escapeHtml`；但一旦未来引入 XSS，`localStorage` 中的管理员 JWT 可被直接读取。响应未统一设置 CSP、`frame-ancestors`、`X-Content-Type-Options`、`Referrer-Policy` 等。
- **建议**：优先使用 Secure/HttpOnly/SameSite cookie 或短时内存 token + 刷新机制；至少增加严格 CSP 和防点击劫持头。

### L-02 每次打开终端都会注册匿名 window resize 监听器，关闭时无法移除

- **位置**：`public/app.js:396-402,467-470`
- **影响**：多次打开/关闭终端后累积监听器，造成不必要调用和内存泄漏。
- **修复建议**：保存 handler 引用，在 `close()` 中 `removeEventListener`。

### L-03 文件目录列表没有条目数和响应大小上限

- **位置**：`agent/rust/src/session.rs:240-269`
- **影响**：列出超大目录会物化全部条目、生成巨大 JSON；超时不会取消已开始的 `spawn_blocking` 操作。
- **修复建议**：分页、最大条目数和续页 token；对超时阻塞任务设置更可靠的隔离方式。

### L-04 Schema 缺少外键和唯一性约束

- **位置**：`schema.sql:22-64`
- **说明**：PAT、审计、系统指标和自定义指标仅用整数关联服务器/用户，没有 FK 或稳定 UUID；`api_tokens.token_hash` 也未显式 UNIQUE。
- **建议**：结合 D1 支持情况增加约束与索引；至少通过迁移和清理任务维护引用完整性。

### L-05 弹窗可访问性不足

- **位置**：`public/index.html:47-294`
- **说明**：弹窗使用普通 `div`，缺少 `role="dialog"`、`aria-modal`、焦点陷阱、Esc 关闭和关闭后焦点恢复。
- **建议**：统一 Modal 组件行为，补充键盘和屏幕阅读器测试。

## 7. 前端专项结论

### 做得好的点

1. 对服务器名、分组、系统信息、探活名、文件名、审计详情等动态 HTML 均使用 `escapeHtml`；本次未确认到可直接利用的 DOM XSS。
2. WebSocket token 不放 URL，避免出现在浏览器历史、代理日志和常规访问日志中。
3. xterm.js、Chart.js 已本地化，避免运行时依赖外部 CDN。
4. REST 封装统一检查 HTTP 状态，敏感新增凭据只显示一次。
5. 监控图表有前端降采样，避免长区间直接渲染过多点。

### 主要改进方向

- 文件传输必须改为有状态、可确认、可恢复且原子的协议，而不是继续在前端修补进度显示。
- token 生命周期和安全响应头需要进入安全基线。
- 增加真实浏览器测试：终端 ready 竞态、上传断线、下载文件变化、重复打开弹窗、键盘可访问性。

## 8. Worker 后端专项结论

### 做得好的点

1. Agent key 使用 SHA-256 指纹定位 + HMAC 哈希校验，避免明文落库。
2. PAT 支持 scopes 和 server_ids，主 API 大部分路径都有权限复核。
3. 终端/文件浏览器 WebSocket 使用首帧鉴权，token 不进入 URL。
4. MCP 有 Origin 校验，并复用 API 权限过滤。
5. 监控查询和审计 limit 有边界，长区间有 SQL 侧抽样。
6. TerminalDO 已将会话元数据持久化，并有 Hibernation/TTL 思路。

### 其他协议问题

- `src/index.js:837-880` 的 MCP/JSON-RPC 未完整验证 `jsonrpc: "2.0"`、batch 与标准 notification 语义，协议版本协商也没有完全使用标准 `params.protocolVersion`。建议以官方 conformance fixture 补测。
- 审计目前只覆盖服务器创建/删除和“打开”终端/文件会话；PAT 创建删除、设置修改、文件读写、登录失败等高价值事件没有完整记录。远程管理面板应补充结构化安全审计，但避免默认记录终端命令内容。
- 多用户登录只提交 password；若两个用户使用相同密码，`findIndex` 固定命中第一人，身份与审计归属错误。应要求 username + password，并使用恒定时间密码验证/成熟身份提供方。

## 9. Rust Agent 专项结论

### 做得好的点

1. 文件 WS 有单消息 8MB 限制，单次读取限制为 512KB，解码缓冲固定为 64KB。
2. 大部分同步文件/系统命令通过 `spawn_blocking` 移出 async runtime。
3. 自定义命令使用独立进程组并启用 `kill_on_drop`，减少超时孤儿进程。
4. systemd 已启用 `NoNewPrivileges`、`PrivateTmp`、内核/控制组保护和地址族限制。
5. 系统命令使用参数数组调用，没有发现把 Agent 上报数据拼入 `sh -c` 的路径；`sh -c` 仅用于管理员显式配置的自定义指标。

### 主要改进方向

- 首先解决 root 权限边界、会话配额、重复 SID 幂等和文件原子写。
- 加入 Rust 单元测试与协议集成测试；当前 Worker 测试无法证明真实 Agent 行为正确。
- `blocking_with_timeout` 只是超时停止等待，不能终止同步 I/O；注释不应把它描述成已防止后台阻塞任务持续运行。

## 10. 测试与验证结果

### 已执行

| 检查 | 结果 |
| --- | --- |
| `npm test` | **通过：73/73** |
| IDE/linter diagnostics：`src/` | 0 |
| IDE/linter diagnostics：`public/` | 0 |
| IDE/linter diagnostics：`agent/rust/src/` | 0 |
| `npm audit --json` | 3 项：1 high、2 moderate |
| `cargo test` | 未执行成功：当前审查环境无 `cargo` |
| `cargo clippy --all-targets -- -D warnings` | 未执行成功：当前审查环境无 `cargo` |

### 测试覆盖优点

- 登录、JWT 篡改/过期、服务器 CRUD、Agent 上报、监控、PAT、设置、审计、MCP 均有 API 测试。
- MetricsDO 覆盖基本读写、trim、归档、保留期和告警。
- TerminalDO 覆盖离线、重试、ready、持久化、TTL、首帧鉴权和控制通道重连。

### 关键缺口

1. 没有“持续 report 不后推 alarm”的测试；现有测试只验证会设置 alarm。
2. 没有模拟 D1 batch 失败并确认 Storage 不删除的数据安全测试。
3. 没有 12h/3d 跨热区-D1 边界的完整性测试。
4. 没有空 PAT 白名单测试；现有测试只测 `[1]`。
5. 没有 exec PAT 从 REST 创建到 WS 首帧鉴权的全链路测试。
6. 没有并发会话配额、重复 SID 幂等、跨连接 cleanup 隔离测试。
7. 没有上传 ACK 背压、断线原子性、offset 错乱、下载中文件变化和浏览器内存测试。
8. 没有 Rust Agent 单元测试。
9. CI 只从当前完整 `schema.sql` 创建新库，没有旧库迁移测试。
10. E2E 对终端竞态通过重复命令规避，应改为断言 ready/缓冲语义。

## 11. 修复优先级建议

### P0：上线前必须完成

1. 删除 `dev-secret` 生产回退，缺少 JWT/HASH secret 时 fail closed。
2. 修复 MetricsDO alarm 不得后推、异常后必须续排。
3. 调整归档顺序为 D1 成功后删除 Storage，并实现有界批处理。
4. 合并热区和 D1 查询，消除 12h/长区间数据空洞。
5. 对终端/文件会话实施配额、绝对 TTL；Agent 对重复 SID 幂等。
6. 将文件上传改为 ACK 背压 + 临时文件原子提交。
7. 生产默认禁用 exec 或使用低权限 Agent 用户；明确 root 模式风险。
8. 为登录提供应用层限流，或强制生产部署 Cloudflare Access。

### P1：首个安全加固迭代

1. 修复空 PAT 白名单和 PAT exec WS 契约。
2. 修复 cleanup 全局清重试、终端输入竞态和失败会话残留。
3. 持久化告警状态，Webhook 实现成功确认/重试。
4. 引入 D1 版本化迁移与升级 CI。
5. 前端改为流式下载；限制目录列表。
6. Agent 强制 WSS，提交 Cargo.lock，发布前执行 Rust 测试。

### P2：持续改进

1. 安全响应头、token 生命周期、可访问性。
2. CI Action 固定 SHA、下载校验、SBOM/attestation。
3. 完整安全审计事件与日志查询能力。
4. 第三方 IP 地理查询改为显式可选。
5. MCP conformance 和跨浏览器端到端测试。

## 12. 建议验收标准

- 缺少任一生产 secret 时，登录、JWT/PAT/Agent 鉴权均不可进入可用状态。
- 任意频率连续上报 30 分钟后，alarm 触发时间不后移，归档和离线检查按期执行。
- D1 写入失败时 DO Storage 数据保持完整；重试后不重不漏。
- 1h、12h、3d、7d、30d 查询均覆盖请求时间范围，热区/D1 边界无缺口和重复。
- 同一 SID 重发 100 次只产生一个 PTY；超配额返回明确错误。
- 500MB 上传的浏览器待发送队列有硬上限；断线不会改变原目标文件。
- Agent 默认权限不能读取/写入系统敏感路径，也不能获得 root shell；root 模式必须显式启用。
- 所有 Critical/High 问题均有可失败再修复的自动化回归测试。

## 13. Bug 分析与修复状态评估

> 本节为后续复核（2026-08-04，对照当前 `main` 代码逐条验证）结论，与上文编号一一对应。
> 「修复状态」截至 2026-08-04 23:59 CST。

### 13.1 Critical

| 编号 | 是否 Bug | 修复优先级 | 当前状态 | 复核说明 |
|---|---|---|---|---|
| C-01 | **是（历史）** | P0 | **已修复** | 复核时当前代码已 fail-closed：`secret()` 缺 `JWT_SECRET` 直接抛错（src/index.js:84-88），登录经 `requireJwtSecret` 返回 503，`hashSecret` 同步抛错；`dev-secret` 已全库移除，unit/api 测试覆盖（`secret({})` 抛错、缺密钥登录拒绝） |

### 13.2 High

| 编号 | 是否 Bug | 修复优先级 | 当前状态 | 复核说明 |
|---|---|---|---|---|
| H-01 | **是** | P0 | **已修复** | `scheduleArchive()` 增加 `getAlarm()` 判断，仅无 alarm 或新时间更早时设置（不后推已有 alarm），且无条件注册保证 `ARCHIVE_TO_D1=0` 时仍清理；新增「高频 report 不后推」回归测试 |
| H-02 | **是** | P0 | **已修复** | 归档改为先收集待删 key，`DB.batch` 成功后再删除 storage 行（D1 失败时行保留、下次重试，两端不丢）；回归测试覆盖 |
| H-03 | **是** | P0 | **已修复** | 热区保留恢复为 12h（alarm 仅删除超 12h 行）；`queryMonitorRows` 统一合并热区+D1（ts 去重、热区优先、超限 JS 降采样），≤12h 完整、长区间不再缺最近 ~1h；新增合并查询回归测试 |
| H-04 | **是** | P1 | **已修复** | 每服务器并发会话上限 8（超限 429）；活跃会话加绝对 TTL（4h，到期强制回收并关闭连接）；新增配额/回收测试 |
| H-05 | **是** | P1 | **已修复** | agent `open_terminal` 幂等（同 SID 已存在只回 `terminal_ready` 不重复 spawn）；`run_terminal` 结束仅删仍指向自己的项（`Arc::ptr_eq`），防旧任务误删新会话 |
| H-06 | **是** | P1 | **已修复** | 前端上传改 stop-and-wait：每块等 `write_result` 确认后再发下一块，队列有界（1 块），大文件不再形成数百 MB 队列 |
| H-07 | **是** | P1 | **已修复** | 临时文件 `{path}.upload.{upload_id}`（上传 ID 唯一，并发同路径不冲突）；严格 offset 校验（必须等于临时文件当前长度，防丢块/乱序）；`write_result` 返回真实 `written` 字节（前端按此累计，不再按块大小推断）；`abort` 指令清理临时文件；会话结束/断线自动清理残留；commit 时 `fsync + rename` 原子替换。手动验证完整上传/丢块拒绝/abort/并发/断线清理均通过 |
| H-08 | **是** | P1 | **已修复** | `queryServersForUser` 改为 `serverIDs == null` 才返回全量，空数组返回空集（与 `canAccessServer` 拒绝语义一致）；新增「未提供=全量/空数组=空集/白名单命中」回归测试 |
| H-09 | **是** | P1 | **已修复** | 应用内置按 IP 失败限流（15min 窗口失败 ≥5 次 → 429 + Retry-After，成功清零，Map 惰性清理）；README 将 Access 升级为「生产必需」（覆盖跨边缘一致性）；新增限流/重置回归测试 |
| H-10 | 是 | P0 | **已修复** | `cf-panel-agent.service` 增加低权限运行选项（注释给出 `useradd` 命令 + `User=`/`Group=` + `DISABLE_EXEC=1` 说明）；默认保持 root 以兼容文件管理，文档提示风险 |
| H-11 | **是（低概率）** | P2 | **已修复** | `handleReport` 写指标前复核服务器仍存在（并发删除时丢弃在途上报，防孤儿指标回写 metrics_custom/热区）；schema 外键部分与 L-04 一致不处理 |

### 13.3 Medium

| 编号 | 是否 Bug | 修复优先级 | 当前状态 | 复核说明 |
|---|---|---|---|---|
| M-01 | **是** | P1 | **已修复** | TerminalDO user-pending 首帧鉴权改用 `authUserByToken` + `canExec`（JWT 管理员直接放行；PAT 需 exec scope + server_ids 白名单，且不享受 creatorUserId 兜底）；新增 PAT 放行/白名单外拒绝/无 exec scope 拒绝测试 |
| M-02 | **是** | P1 | **已修复** | `pendingTerm` 记录 `serverId`+`agentWs` 归属；`cleanup` 仅清理关联项（会话数据流断开清该会话、控制通道断开清该 serverId），其他服务器/会话的终端重试不受影响；新增跨服务器/会话隔离测试 |
| M-03 | **是（设计）** | P2 | **已修复** | 新增 `agentBuf`（上限 128 条）：agent 数据流未挂接时缓冲浏览器输入，挂接后 `attachAgentFlow` 按序补发；新增缓冲/补发测试 |
| M-04 | **是** | P1 | **已修复** | `/rpc create` 先确认 agent 在线，离线直接 502 且**不落盘**（内存/storage 无残留）；创建成功即安排 TTL 回收 alarm；新增「离线不残留」测试 |
| M-05 | **是** | P2 | **已修复（部分）** | alarm 归档改为分批 `batch`（单次 ≤100 条，防归档积压/多服务器时超大 batch 超 D1 限制），并保持「D1 成功后再删 storage」；`storage.list` 全量物化暂保留（数据量受 12h 热区上限约束） |
| M-06 | **是** | P2 | **已修复** | `metrics_min` 增加 `mem_total` 列，alarm 归档与 D1 历史查询均带该列；README 补充旧库 `ALTER TABLE` 迁移命令；归档/合并测试断言 mem_total |
| M-07 | 部分（影响小） | P2 | **已修复** | 告警冷却（`alert:*`）与探活去重（`probe:*`）状态在**变更时**持久化到 DO Storage（内存为主），实例 evict 后惰性恢复，`/drop` 同步清理；新增持久化/恢复/清理测试 |
| M-08 | **是** | P1 | **已修复（简化）** | `sendWebhook` 检查 `response.ok`，非 2xx / 网络异常 `console.error` 记错误日志（Cloudflare 后台 Worker 日志可见，便于排查丢失的告警）；按用户要求未采用持久化 outbox（失败不自动重试），新增成功/失败/异常测试 |
| M-09 | **是** | P2 | **已修复** | `sendWebhook` 统一允许 GET/POST/PUT（PUT 不再静默改 POST，携带 body；非法方法回退 POST）；新增 PUT/GET/非法方法测试 |
| M-10 | **是（前端）** | P1 | **已修复（部分）** | 下载改为每块立即解码为 `Uint8Array` 并直接 `new Blob(parts)`（消除完成时的 join 与二次复制），500MB 级峰值内存大幅下降；尚未采用 File System Access/StreamSaver 全流式 |
| M-11 | **是** | P1 | **已修复** | 前端 `onReadResult` 收到 `got===0` 且未达预期 size 时中止下载并提示（文件已缩短/替换），不再无限循环请求同 offset |
| M-12 | **是** | P1 | **已修复** | Rust agent 启动校验 `AGENT_WSS_URL` scheme：远程强制 `wss://`，明文 `ws://` 仅限本地回环（127.0.0.1/localhost/::1）或 `ALLOW_INSECURE_WS=1`；手动验证 loopback 放行、远程明文拒绝 |
| M-13 | **是** | P1 | **已修复** | 新增 Rust 单元测试（validate_wss 含 IPv6 `::1` 回环修复、base64/err_json/缓冲常量、/proc 解析）共 9 个；`agent-rust.yml` 增加 test job（`cargo fmt --check` + `cargo clippy --all-targets -- -D warnings` + `cargo test`），build-release `needs: test`——测试不过不再发布；顺带全量修复 6 处 clippy 警告 |
| M-14 | **否（已过时）** | — | **已满足** | `agent/rust/Cargo.lock` 已在仓库跟踪（`git ls-files` 确认），复核时已不成立 |
| M-15 | **是** | P1 | **已修复** | 引入 `wrangler d1 migrations`：`migrations/0001` 初始建表 + `0002` 追加 mem_total；README 建表/迁移改为 `apply`，Workers Builds Deploy command 配置 `npx wrangler d1 migrations apply && npx wrangler deploy`（push 自动迁移建表+部署，免手动 SQL）；E2E 改用 migrations 路径验证 |
| M-16 | **是（已复核）** | P1 | **已修复** | `package.json` 用 `overrides` 强制 `undici@^7.29.0`，`npm audit` 归零；E2E 验证 wrangler/miniflare 兼容无回归 |
| M-17 | **是（增强）** | P2 | **已修复（部分）** | Actions 固定具体版本（checkout@v4.2.2 / setup-node@v4.2.0）；权限按 job 最小化（test 只读、build-release 才写）；websocat 下载加 `--fail --retry`；Release 附带 sha256 校验和。未做：action SHA 级固定、SBOM/attestation |
| M-18 | **是（隐私）** | P2 | **已修复** | 站点设置新增 `geo_lookup` 开关（**默认关闭**，前端不再默认把服务器公网 IP 发 ipapi.co/ipwho.is）；站点信息弹窗可开启；public settings 返回开关；CSP 保留两域名（开启时放行） |

### 13.4 Low

| 编号 | 是否 Bug | 修复优先级 | 当前状态 |
|---|---|---|---|
| L-01 | **是（建议）** | P2 | **已修复** | `json()` 统一附加 `X-Content-Type-Options`/`Referrer-Policy`/`X-Frame-Options`/`CSP(frame-ancestors 'none')`；`index.html` 加 CSP meta（本地 JS/CSS + WebSocket + IP 地理放行）；新增安全头测试 |
| L-02 | **是（建议）** | P3 | **已修复** | 终端 resize 监听器改为命名函数，`close()` 时 `removeEventListener`（防多次开关累积泄漏） |
| L-03 | **是（建议）** | P3 | **已修复** | agent `file_list` 加 1000 条上限 + `truncated` 标记，前端显示截断提示（防超大目录物化巨型 JSON） |
| L-04 | 是（建议） | P3 | **不处理（用户决策）** | 表较少且简单，暂不加外键/稳定 ID；删除服务器时由删除接口显式清理关联表（metrics_min/metrics_custom/audit_logs） |
| L-05 | **是（建议）** | P3 | **已修复** | 弹窗统一注入 `role="dialog"`/`aria-modal="true"`；Esc 关闭（触发关闭按钮走既有清理逻辑，如终端 ws/resize）；MutationObserver 焦点管理（打开聚焦首个可聚焦元素、关闭恢复焦点到触发元素）。未做全局焦点陷阱（避免干扰终端/文件输入） |

### 13.5 复核结论

1. **全部 35 项中 33 项成立、1 项过时不成立（M-14）、1 项维持现状（M-07）**。
2. **本会话已另行修复、未列入原 review 的问题**（同样属于数据安全/可靠性）：
   - MetricsDO 热区改 DO Storage 持久化（实例 evict 后监控数据不再丢失）；
   - `ARCHIVE_TO_D1=0` 时 alarm 无条件注册与清理（storage 热区 / audit_logs 不再无限增长）；
   - E2E 默认 agent 切换为 Rust 版、终端用例失败自动重试。
3. **P0 四项已于本次复核后修复**：C-01 复核确认已 fail-closed（`dev-secret` 全库移除 + 测试覆盖）；H-01/H-02/H-03 本次完成（alarm 不后推、D1 成功后再删 storage、热区保留 12h + 热区/D1 合并查询），均含回归测试。**H-07/H-08/H-09/H-10/M-01~M-18/L-01~L-03/M-14(过时) 已处理**（含文件上传原子写、空 PAT 白名单、登录限流、service 低权限、PAT WS 鉴权、pendingTerm 隔离、agent 输入缓冲、离线会话回滚、归档分批、mem_total、告警状态持久化、webhook 失败日志、下载内存优化、下载 got=0 中止、Agent 强制 WSS、Rust 测试进 CI、D1 migrations、npm audit 归零、安全响应头/CSP、resize 监听器、目录列表上限、CI 供应链加固、IP 隐私开关），当前 JS 91/91 测试 + Rust 9/9 测试通过、E2E 8/8。**35 项全部有结论**：34 项已修复/处理、M-14 过时、L-04 用户决策不处理（含此前遗留的 H-04/H-05/H-06/H-11，现已全部补齐——Critical 与 High 共 12 项全部完成）。

## 14. Cloudflare 额度消耗分析（2026-08-05 按 HEAD=8b18ede 复算，方案 1-4 落地后）

> **计量口径（2026-08-05 官方文档）**：Workers Free：10 万请求/天、CPU 10 ms/请求、D1 5M 行读 + 10 万行写/天、存储 5GB；Durable Objects Free（SQLite 后端）：10 万事件/天、13,000 GB-s/天、DO Storage 5M 行读 + 10 万行写/天、存储 5GB。DO 的 HTTP 请求、alarm、Worker 到 DO 的 `stub.fetch` 与 WebSocket 消息都计入事件；**入站 WS 消息按 20:1 折算为计费请求**，连接建立另计 1 次；Hibernation 休眠不计时长，事件处理期间按 128MB 墙钟计费。Workers Assets 与出站流量不计上述额度。
>
> **事件双口径**：「保守口径」按 1 事件/帧计（便于上限估算）；「实计口径」将 Agent→TerminalDO 的入站 WS 消息按 20:1 折算（更接近账单）。
>
> **基线参数**：`S` = 服务器数，`V` = 同时可见的观看者数，`C` = 每机自定义指标数。Agent 有观看者时 **5 秒**快采（17,280 帧/天/机，`44`），无人观看时 120 秒慢采（720 帧/天/机）；**批量上报**（`433-435, 501-524`）：TerminalDO 分片 isolate 内 `reportBatch` 聚合同机 5s 窗口内最后一帧，距上次 flush ≥5s 时**全分片多机合并为一次 fetch**——单机每帧 flush ≈17,280 次/天/机，**多机封顶 17,280 次/分片/天（S≥4 时全局 ≤4×17,280=69,120）**；**推送** `LATEST_PUSH_INTERVAL_MS=5000`（`19`），MetricsDO 单例上限 **17,280 次/天**；自愈反查 30s 节流（≤2,880 次/天）；alarm 活跃 144 次/天、闲置 24 次/天；`fullSweep` ≈24 次/天；prune 1 次/天（走 `idx_metrics_custom_ts`）。行号均指当前 `src/index.js`。

### 14.1 分项额度消耗（当前实现，方案 1-4 后）

| 资源 | 当前实现 | 周期消耗（1 台机，慢采/无观看者） | 周期消耗（1 台机，快采/1 观看者） | 额度敏感性 |
|---|---|---:|---:|---|
| Worker HTTP/WS 请求 | 前端静态页/API 按需；后台标签页关闭 WS；失效 Agent 401 后 300s 长退避（约 288 次/天/坏 Agent） | 约数百/天 | 约数百/天 | **低** |
| Worker CPU | JWT/HMAC、JSON、SQL 编排；列表 2s 缓存；单请求毫秒级 | 可忽略 | 可忽略 | **低** |
| DO 事件：Agent 上报链 | 入站消息（实计 20:1）+ 分片批量 fetch（单机每帧 flush 17,280/天/机；**多机 ≤17,280/分片/天，S≥4 全局封顶 69,120**）+ 自愈反查（无观看者时，单机慢采每帧 = 720/天） | 720 + 720 + 720 = **2,160/天**（实计 ~1,476） | 17,280 + 17,280 = **34,560/天**（实计 864 + 17,280 = 18,144） | **中**（多机不再随 S 线性增长） |
| DO 事件：Panel 推送 | MetricsDO→PanelDO ≥5s 节流（上报驱动，与 `V` 解耦）；前端 onopen 一次 sync + 被动接收，1s 本地在线老化 | 0 | **~17,280/天**（单机跟随帧率；多机封顶同值） | **中**（多观看者零边际） |
| DO 事件：alarm / fullSweep | alarm 144 次/天（活跃）或 24 次/天（闲置退避）；fullSweep ≈24 次/天（持久化时间戳）；TerminalDO 仅会话回收需要时 | 144/天 | 144/天 | **低** |
| DO 事件：终端/文件 | 入站帧按 20:1 计费；Agent PTY 16ms/32KB 合帧，文件 1MB 分块 | 按需 | 按需 | **中**（使用驱动） |
| DO 时长 | Hibernation 下只有处理帧/推送/告警的 128MB 墙钟 | 通常 <1 GB-s/天 | 通常约 1~5 GB-s/天 | **低** |
| DO Storage 写 | `m:` put 按分钟去重（1,440/天/机，与帧率无关）；12h 后 delete 同量；水位 `arc:*`（实例存活每分钟推进 1,440；慢采 evict 场景由 fullSweep 推进 ≈24）；usage 全零跳过；housekeep 仅变化时写 | 720 + 720 + 24 + ~170 ≈ **1,600 行/天** | 1,440×3 + ~170 ≈ **4,500 行/天** | **低~中**（S=15 快采 ~67,500 行/天 = 写档 68%，多机时转为主要项） |
| DO Storage 读 | 兜底 list 仅 evict 后首帧跨线 + 重试标记，常态分钟跨线零读；fullSweep 24 次 × `720×S` 行；arc/housekeep/usage get（慢采 ~1,000/天，快采 ~300/天） | **~18,300 行/天** | **~17,600 行/天** | **低** |
| DO Storage 容量 | 热区最多 `720×S` 行，告警/会话/水位/用量键少量 | 很小 | 很小 | **低** |
| D1 读行 | 上报 SELECT 经 `serverRowCache` 60s（快采 ~1,440 行/天/机，慢采 720）；settings 300s TTL + RPC 失效；**Panel 列表由推送驱动**：push 5s 间隔 > listCache 4.5s TTL → 每次 push miss 读全表 `S` 行 = `17,280×S` 行/天 | 720 + ~860 ≈ **1,600 行/天** | 1,440 + 288 + 17,280 ≈ **19,000 行/天** | **中**（多机时列表读线性增长：S=15 约 26 万行/天） |
| D1 写行：last_seen | `info_json` 变化必写，否则 10s 节流（5s 帧 → 每 2 帧写一次 ≈8,640/天/机） | 720 行/天/机 | **~8,640 行/天/机** | **中~高**（多机全天大屏的最大可控项） |
| D1 写行：指标 | `metrics_min` 增量归档（快采 1,440 行/天/机；慢采 ~720）；`metrics_custom` 分钟去重 `1,440×C`（慢采 ~720×C） | 约 `720×(1+C)` 行/天 | 约 `1,440×(1+C)` 行/天 | **中**（数据本质，不可再降） |
| D1 写行：prune/audit/设置 | prune 每天 1 次，滚动删除量 ≈ 日新增量；走 ts 索引；audit 低频 | ≈ 日新增量 | ≈ 日新增量 | **中**（随 S 线性） |
| D1 容量 | `metrics_min`：`1,440×S×R` 行；`metrics_custom`：`1,440×S×C×R` 行；audit 90 天清理 | 约数 MB/机 | 约数 MB/机 | **低~中** |
| 网络 egress | Workers/DO/D1 不收出站流量费；PanelDO→前端广播出站免费 | 0 | 0 | **低** |
| 外部 Webhook | 仅告警时出站 fetch；未配置 webhook 时离线扫描完全跳过 | 低频 | 低频 | **低** |

### 14.2 典型场景汇总（当前实现，方案 1-4 后）

| 场景 | Worker 请求 | DO 事件/天（保守 / 实计） | DO Storage 读/写 | D1 读/写 | 结论 |
|---|---:|---:|---:|---:|---|
| 1 台机，慢采，无观看者 | <1,000 | **约 2,304 / 1,620** | 读 ~18,300；写 ~1,600 | 读 ~1,600；写 ~2,900（C=1） | 远低于免费档，可长期常驻 |
| 1 台机，快采 + 1 观看者全天 | <2,000 | **约 52,000 / 35,600**（入站 17,280 + fetch 17,280 + push 17,280 + alarm 144） | 读 ~17,600；写 ~4,500 | 读 ~19,000；写 ~14,400（C=1） | 档内（35.6%）；比上一版贵，换卡片 ~6s、告警 5s、宽限余量充足 |
| 5 台机，快采 + 1 观看者全天 | <3,000 | **约 172,900 / 90,900**（入站 86,400 + fetch 69,120 + push 17,280 + alarm 144） | 读 ~88,000；写 ~22,500 | 读 ~93,600；写 ~72,000（C=1） | 实计 ~91% 档内；fetch 封顶尚未摊薄（5 机 < 4 分片×2） |
| 10 台机，快采 + 1 观看者全天 | <4,000 | **约 259,300 / 95,200**（入站 172,800 + fetch 69,120 + push 17,280 + alarm 144） | 读 ~175,000；写 ~45,000 | 读 ~187,000；写 ~115,200（C=0） | **实计进档（95%）**；D1 写略超（需 last_seen 节流 30s → ~57,600 ✓） |
| 15 台机，快采 + 1 观看者全天 | <5,000 | **约 345,700 / 99,500**（入站 259,200 + fetch 69,120 + push 17,280 + alarm 144） | 读 ~262,000；写 ~67,500（写档 68%） | 读 ~281,000；写 ~172,800（C=0） | **实计刚好进档（99.5%）**；D1 写 C=0 需 last_seen 30s 节流（→86,400 ✓），C≥1 需降粒度或付费档 |
| 每多 1 台快采 Agent（S≥4 后） | 0 | **+17,280 / +864**（fetch/push 已双封顶，不再增长） | 读 +17,280、写 +4,500 | 读 +~18,700、写 +11,520（C=0） | **DO 事件边际成本已解耦，剩余线性源转为行读写** |
| 每多 1 名可见观看者 | +1 WS | **≈0**（推送与 V 解耦；广播出站免费；缓存共享） | ≈0 | ≈0（仅 per-ws 权限过滤 CPU） | 多观看者近零边际成本 |
| 每多 1 个失效 Agent key | 约 +288 | 0（Worker 阶段 401） | 0 | 约 +288 读 | 静默成本已降约 99% |
| 闲置面板（无 Agent 无观看者） | 极低 | **约 24/天**（闲置退避） | 读 ~48；写 ~0 | 读 ~24；写 0 | 接近零成本常驻 |

### 14.3 当前瓶颈与结论

1. **方案 1-4 的设计转向**：用单机/小规模的事件预算换实时性（单机 fetch/push 17,280/天 vs 上一版 7,200，换来卡片 ~6s 新鲜度、告警/探活 5s 判定、`last_seen_s` 最旧 ~6s 远低于 15s 宽限），同时用分片批量把多机 fetch 封顶在 `4×17,280=69,120`。**交叉点约 9~10 台：S≤8 比上一版略贵，S≥10 开始受益**。
2. **DO 事件的机器数线性已消除**（用户 10~15 台场景的关键收益）：S≥4 后每多 1 台仅 +864 实计事件/天（入站），fetch/push 双封顶。15 台全天大屏实计 ~99,500（上一版 158,500，超档 59% → 刚好进档）。
3. **瓶颈已转移至行读写**：
   - **D1 写**（10+ 台全天大屏的首要约束）：last_seen 8,640/机/天是最大可控项——`LAST_SEEN_THROTTLE_S 10→30`（一行改动，D1 last_seen 仅 MetricsDO 冷启动兜底，无宽限约束）后 15 台 C=0 进档（~86,400）；C≥1 时指标+prune 的 `43,200×(1+C)`/天是数据本质，需降 custom 粒度/数量或付费档。
   - **DO Storage 写**：S=15 快采 ~67,500 行/天（写档 68%），S=20 将达 90%，是下一个需要关注的项（可降低水位推进频率或 fullSweep 删除批量化）。
   - **D1 列表读**：push 5s 间隔 > listCache 4.5s TTL → 每次 push miss 读全表（`17,280×S` 行/天，S=15 约 26 万行 = 读档 5.2%）；如需优化可将 listCache TTL 提至 ~6s（与 push 节拍对齐）。
4. **不建议为省事件把 flush 节流从 5s 再调高**：当前 5s 帧恰好对齐 5s 窗口是实时性设计的 intentional 选择；若未来接受更低实时性，快采 60s + 宽限 90s（2 行）仍是最便宜的深度降额路径（见第 16 章方案 5 分析）。
5. **观测**：`/api/usage` + 设置页「用量观测」面板（report/latest/query/alarm 计数与估算）；多 Worker 共账户部署时按 14.2 表预留余量。

## 15. 在保证安全与正确前提下的降额优化

> 优先级按“收益/改动复杂度/正确性风险”排序。P0/P1 均不改变鉴权、权限边界、数据保留语义或 Agent 协议；不要求删除 DO，目标是让现有架构在免费档内更久运行。

### 15.1 P0：直接解决最可能超限的路径

1. **alarm 改为增量归档与逐行清理** ✅ 已实施（2026-08-05）：`/report` 热写路径按归档水位 `arc:{serverId}` 增量归档（跨线行直接归档 + 水位区间批量），D1 成功后才推进水位；`alarm()` 仅做离线告警/保留期清理，全量 `fullSweep` 降频为每 6 次（≈1 小时）执行（>12h 行清理 + 兜底归档），DO Storage 读约 `103,680×S` → `~17,280×S`/天，重复 INSERT 基本消除。**修复（同日）**：水位改为只推进到本次实际归档的最大 `ts`（不再无脑推进到归档线），区间扫描限 `METRICS_KEEP_MIN` 次（≤720/帧）——修复热写免 loadHot（#8）引入的 evict 后空 Map 导致「storage 历史行被水位越过 → fullSweep 永久跳过 → 12h 删除 → 3d/7d/30d 历史空洞」回归，以及新服务器 `arcTs=0` 从分钟 1 空循环约 4000 万次的 CPU 尖峰；未覆盖区间由 `fullSweep` 全扫兜底（含回归测试）
   - 当前：`alarm()` 每 10 分钟全量 `list('m:')`，并对 60min~12h 已归档行重复 `INSERT OR IGNORE`（`1892-1955`）。
   - 建议：`/report` 写入内存热区时，直接对本次内存 Map 中 `ts <= archiveCutoff` 的少量行做 D1 `INSERT OR IGNORE`、对 `ts <= keepCutoff` 的键直接删除；持久化每台机的归档水位（如 `arc:{serverId}=lastArchivedTs`）。`alarm()` 只执行离线告警、保留期清理和低频兜底，不再常态全扫 720×S 行。
   - 预期：DO Storage 读约 `103,680×S` → 数百~数千行/天；重复归档查询约 95,000 条/天/机 → 增量后实际新增约 10 条/10 分钟；alarm 时长显著下降。
   - 正确性约束：D1 成功后再删除 Storage；水位仅作加速，不替代 D1；evict 后从水位恢复并允许重复 `INSERT OR IGNORE` 兜底。

2. **`last_seen` 节流并优先使用 MetricsDO 最新指标判定在线** ✅ 已实施（2026-08-05）：`handleReport` 中 `info_json` 变化时无条件写，否则按 `LAST_SEEN_THROTTLE_S=10` 节流落盘（快采 28,800 → ~5,760 行/天/机，−80%；最旧 ~12s 仍低于 15s 宽限）；MetricsDO 内存维护秒级 `lastSeenSec`（每次 `/report` 热写更新，`/latest` 输出 `last_seen_s`），`/api/servers`、MCP `list_servers`、PanelDO sync 三处在线判定优先用 `last_seen_s` 主判，D1 `last_seen` 仅 MetricsDO 无该机数据（冷启动/evict）时兜底
   - 当前：每次上报都写 `servers.last_seen`（`427-433`），快采 28,800 行/天/机；列表每次读 D1 last_seen。
   - 建议：`handleReport` 节流写 D1。**注意在线宽限 `ONLINE_GRACE_FAST_S=15`：节流粒度须 ≤10 秒（快采 3~4 次落盘一次，最旧约 12s 仍低于 15s 宽限），或在线判定改由 MetricsDO `/latest` 的 `metric.ts` 主判、D1 `last_seen` 仅冷启动兜底**，避免 last_seen 恰落在离线判定边界造成误判。快采节流后可降至约 5,760 行/天/机（−80%）。
   - 预期：D1 写行数从“4 台快采超免费档”降为约 20 台内可用；Panel sync 对 D1 的读取同时减少。
   - 正确性约束：D1 仍保留审计/离线告警所需时间戳；MetricsDO 故障时回退 D1，不改变权限与在线语义。

3. **去掉每次上报后的 PanelDO `/viewers` 查询** ✅ 已实施（2026-08-05）：观看者数变化由 PanelDO 事件驱动广播 `/rpc/set_viewers`（0→1 快采 / 1→0 慢采，含 `webSocketClose` 兜底最后观看者下线）；TerminalDO `syncAgentInterval` 上报时仅读内存 `agentInterval`，仅实例 evict/新连接时查 `/viewers` 兜底。上报链 DO 事件从 3 → 2/帧（快采 86,400 → 57,600 事件/天/机）
   - 当前：`syncAgentInterval()` 每次上报都调用 PanelDO（`1467→1516-1526`），占上报链 1/3 DO 事件。
   - 建议：观看者状态采用事件驱动：PanelDO 在 0→1、1→0 观看者变化时向 4 个 TerminalDO 分片广播；TerminalDO 内存维护当前模式；`/viewers` 仅低频兜底或删除。
   - 预期：上报链从 3 事件/帧降为 2 事件/帧；快采 Agent 从 86,400 → 57,600 事件/天/机。
   - 正确性约束：首个观看者上线仍立即切快采；观看者断开后最迟一个 120 秒周期恢复慢采；仅改变调度，不改变上报数据。

4. **PanelDO 服务器列表与鉴权缓存** ✅ 已实施（2026-08-05）：服务器列表缓存 3s TTL（多观看者共享）+ 鉴权缓存 5s TTL（JWT 无 D1 读、PAT 降频；删除后 ≤5s 失效，未做 RPC 即时失效）；权限过滤仍在缓存之上执行（不缓存可越权结论）。多观看者 D1 读从 `28,800×S×V` 大幅下降
   - 当前：每个观看者每 3 秒重新 `authUserByToken`（PAT 每次读 D1）、读全量 `servers`、调用 `/latest`（`1995-2043`）。
   - 建议：PanelDO 内存缓存服务器列表 2~5 秒，多观看者共享；JWT 鉴权结果随 attachment 缓存，PAT 鉴权结果按短 TTL 缓存并随 token 删除失效；服务器增删改时通过内部 RPC 使缓存失效。
   - 预期：多观看者 D1 读从 `28,800×S×V` 降为约 `5,760~9,600×S/天`，PAT 观看者成本显著下降。
   - 正确性约束：PAT 删除必须即时失效；服务器权限过滤仍在每条响应前检查；缓存只作用于静态列表，不缓存 `canAccessServer` 结论到可越权对象。

### 15.2 P1：显著降低免费档压力

5. **MetricsDO `/latest` 使用增量 latest Map** ✅ 已实施（2026-08-05）：`/report` 热写 O(1) 维护 `latestByServer`（含秒级 `last_seen_s`），`/latest` 直接 O(S) 返回；仅实例 evict 后（增量 Map 为空）全扫 storage 恢复并回填。`/drop` 同步清理增量项
   - 当前：每次 `/latest` 对所有缓存服务器扫描分钟 Map；evict 后一次全量 `list('m:')`（`1684-1717`）。
   - 建议：`/report` 时 O(1) 维护 `latestByServer`；`/latest` 直接 O(S) 返回；evict 后仅按需 `loadHot` 或保留低频全扫兜底。
   - 预期：消除 evict 后 `720×S` 行尖峰，并避免快采期间 CPU 抖动；顺带让观看者查询更稳定。

6. **MetricsDO `/report` 避免每次 `ensureHot/loadHot` 和每次 `getAlarm`** ✅ 已实施（2026-08-05）：热写改用“缺省空 Map + 新分钟 key”（evict 后不再每帧全量 loadHot ~720 行，历史恢复由 `/query`、`/latest`、`fullSweep` 兜底）；`alarmCached` 缓存下一次 alarm 时间，`getAlarm` 从每帧 1 次降至每周期 1 次
   - 当前：上报时若实例 evict 会先读该服务器全部 720 行；随后再 `getAlarm` 一次（`1655-1664`, `1760-1767`）。
   - 建议：`/report` 热写使用“缺省空 Map + 新分钟 key”即可，只在 `/query`/`/latest`/alarm 真正需要历史时加载；实例内缓存当前 alarm 时间，仅在未知或过期时 `getAlarm`。
   - 预期：慢采 evict 后每帧少读约 720 行；每次上报少 1 行 storage 读。
   - 正确性约束：Storage 仍是唯一事实源；查询路径保留恢复逻辑。

7. **自定义指标 `metrics_custom` 与热区写入按分钟去重** ✅ 已实施（2026-08-05）：`customWritten` 模块级记录每台机 `{minTs, names}`，同分钟同指标不再执行 `INSERT`（仅新指标名或跨分钟才写，`INSERT OR IGNORE` 幂等兜底；evict 丢失后偶发多写一次无害）
   - 当前：快采同分钟重复执行 `INSERT OR IGNORE`，`m:` put 已按分钟去重但 custom 仍随帧批量执行（`416-423`）。
   - 建议：TerminalDO/MetricsDO 内存记录每台机当前分钟是否已写 custom，只有 minTs 变化或值变化时写 D1。
   - 预期：D1 写查询约 −95%（快采场景），指标语义仍为分钟粒度。

8. **合并 `handleReport` 中的服务器读取与存在性复核** ✅ 已实施（2026-08-05）：单条 `SELECT info_json, probe_json, name` 同时完成存在性判断（D1 读 2 → 1 行/上报，快采省 28,800 行/天/机）；删除并发窗口由 MetricsDO `/drop` 清理热区 + `INSERT OR IGNORE` 孤儿行无害承担
   - 当前：先读 `info_json/probe_json/name`，再单独复核 `id`（`400-407`）。
   - 建议：使用一条 `SELECT info_json, probe_json, name FROM servers WHERE id=?` 的结果同时完成存在性判断，或迁移为条件写。
   - 预期：上报 D1 读行从 2 行降为 1 行；快采节省 28,800 行/天/机。
   - 正确性约束：不能回到“读一次后盲写”的竞态；删除并发仍以 `handleReport` 复核与 MetricsDO `/drop` 兜底。

9. **Agent 控制通道重连指数退避与 401 慢退避** ✅ 已实施（2026-08-05，Rust agent）：连续失败指数退避 `3s→6s→…→300s` 封顶 + 0~50% 抖动（防重连风暴）；401 鉴权失败直接 `AUTH_FAIL_SECS=300` 长退避（28,800 → ~288 请求/天，静默成本降约 99%）；正常断开重置回 3s 快速恢复；`is_auth_error` 识别 tungstenite `Error::Http(401)` 并有单测
   - 当前：Agent 固定 3 秒重连；失效 key 每天造成约 28,800 Worker 请求和 28,800 D1 行读。
   - 建议：Rust/Shell Agent 对连续失败采用 3s→30s→5min 封顶退避并加抖动；服务端对 401/删除服务器的控制连接返回明确 close code，Agent 对鉴权失败使用更长退避或退出。
   - 预期：失效 Agent 的静默成本降低约 99%。
   - 正确性约束：正常网络抖动仍可自动恢复；不删除手动重启 Agent 的恢复能力。

10. **后台标签页暂停 Panel sync** ✅ 已实施（2026-08-05）：`visibilitychange` 隐藏时 `stopPushTimer` 暂停 3s sync，恢复可见时立即拉取一次并恢复定时；连接断开由既有 `startPush` 重连（可见时兜底重建）
    - 当前：前端打开标签页即每 3 秒发送 `sync`。
    - 建议：`document.hidden` 时停止 `pushTimer`，恢复可见时立即拉取一次；或使用 `visibilitychange` 降低为 15~30 秒。
    - 预期：多开/后台标签页成本接近归零；实时交互标签页体验不变。

### 15.3 P2：规模更大时的容量与延迟优化

11. **终端/文件输出批量合帧** ✅ 已实施（2026-08-05，Rust agent）：PTY 输出聚合 `TERM_BATCH_MS=16ms` 或 `TERM_BATCH_BYTES=32KB` 合并一条 WS 帧（刷屏时 DO 计费消息约 −60%~75%，交互延迟 ≤16ms 无感、顺序保持）；文件 `READ_BLOCK` 512KB → 1MB 与前端下载分块对齐（一次 read 返回 1MB，下载帧数 −50%；前端按 `got` 自适应续传，兼容）
    - 当前：Rust Agent PTY 每 8KB 一帧，文件下载 512KB 一轮；每条入站帧按 20:1 计费。
    - 建议：Agent 按 10~20ms 或 32~64KB 聚合输出；文件 `READ_BLOCK` 与前端 1MB 块对齐。
    - 预期：刷屏/大文件传输的计费消息数约降 50%~75%；注意文件协议的正确性应优先于合帧。

12. **MCP/API 列表复用 PanelDO 缓存** ✅ 已实施（2026-08-05）：抽取公共 `listServersWithState(env, user)`（`/api/servers` 与 MCP `list_servers` 共用），按用户维度 2s TTL 短缓存（权限过滤仍在 SQL 层，不缓存越权结论；服务器增删改时显式 clear，最长滞后 2s）；多入口不再各自读 D1 全表 + `/latest` + `/viewers`
    - 当前：`/api/servers`、MCP `list_servers` 都单独读取 D1、`/latest` 和 `/viewers`（`664-677`, `884-920`）。
    - 建议：低风险场景沿用短 TTL 列表缓存，或抽公共读取函数；MCP 可支持分页/摘要，避免 AI 客户端循环拉全量。
    - 预期：多入口重复读降低；对免费 Worker/DO 额度更友好。

13. **离线告警只在需要时调度** ✅ 已实施（2026-08-05）：未配置 `webhook_url` 时直接跳过离线扫描（零成本）；在线判定复用增量 `latestByServer` 秒级 `last_seen_s`（与列表判定一致）；离线状态一次性 `list('alert:offline:')` 惰性加载到内存，不再逐机 get（S 大时免 144×S 行读）
    - 当前：告警开启时 alarm 每 10 分钟读全部 servers 并逐机读取离线状态（`1856-1888`）。
    - 建议：离线判定直接利用上报路径和 MetricsDO latest；alarm 在未配置 webhook 时跳过离线扫描；离线状态加载采用一次性 list 并按 server 惰性恢复。
    - 预期：S 大时减少 144×S 行 D1/storage 读/天；未配置告警时几乎零成本。

14. **保留期与存储增长预警** ✅ 已实施（2026-08-05）：`METRICS_RETENTION_DAYS` 支持环境变量覆盖（默认 30，缩短线性降容量）；前端自定义指标设置弹窗加容量提示（每机 ≤20 个，占用 ≈ 43,200×S×C 行）；README 新增「免费额度与容量估算」小节（含公式与 `/api/usage` 入口）
    - 当前：`metrics_min` 30 天约 `43,200×S` 行；`metrics_custom` 再乘 `C`；5GB 免费存储在数百台或大量自定义指标下仍可触顶。
    - 建议：为 `C` 设置推荐上限并在 UI 提示；提供 `METRICS_RETENTION_DAYS` 可配置（默认 30 天）；部署文档给出按 S×C 估算的容量公式。
    - 预期：避免从“请求/行数可用”突然变为“存储超限无法写入”。

15. **监控与告警自身的额度观测** ✅ 已实施（2026-08-05）：`GET /api/usage`（仅管理员）聚合 Worker 请求计数（`apiCounts`，实例级）+ MetricsDO 用量（`/report`、`/latest`、`/query`、alarm 每 10 分钟汇总到 storage `usage:total`，跨 evict 保留），并给出近 24h 上报帧 / DO 事件 / D1 写行估算
    - 建议：在设置页展示近 24 小时的估算用量：DO 事件（上报帧、sync 帧、alarm）、D1 读写行、DO Storage 读写行；至少通过日志/Admin API 定期统计。
    - 预期：部署者在多 Worker 共享免费额度时能提前发现 cf-panel 占额异常，而不是超限后服务失败。

### 15.4 不建议的“省费”方式

1. **不建议把热区彻底迁回 D1**：每次上报 D1 写行与读行会立即把免费写档击穿，正确性和成本都更差。
2. **不建议取消 alarm**：alarm 仍承担离线告警、保留期清理与兜底归档；应改为增量和低频兜底，而不是删除。
3. **不建议删除 DO Hibernation 或改长轮询 HTTP**：长轮询会产生更多 Worker/DO 请求和墙钟时长，成本更高。
4. **不建议为省行数删除审计或弱化鉴权**：安全与权限边界优先级高于额度；可用缓存和事件驱动降耗。
5. **不建议默认降低上报到分钟级**：会改变实时面板体验；应先做事件驱动调度、缓存和增量归档。

### 15.5 优化后的目标量级（实施 P0 后估算）

- 1 台机快采 + 1 观看者全天：DO 事件约 **9 万/天以内**（report 2 事件 + sync 2 事件），回到免费档边缘；若再做后台标签页暂停，常态明显更低。
- D1 写行：last_seen 约 **5,760 行/天/机**，指标约 1,440 行/天/机，重复归档基本消除；5 台快采可从 >15 万/天降至约 3.6 万/天。
- DO Storage 读：alarm 全扫从 `103,680×S` 行/天降为增量读取；`/latest` evict 尖峰消除。
- 结论：在不牺牲鉴权、权限过滤、原子写、幂等重试与监控正确性的前提下，P0 四项即可把最主要的 DO/D1 免费档压力降低约 40%~70%，P1/P2 进一步为多 Agent、多观看者和多 Worker 共账户部署留出余量。

## 16. 二次降额复核（2026-08-05，基于 HEAD=9623216 逐行实测）

> 本章由两个独立只读复核 + 主代理逐行验证产生。结论：第 15 章优化方向全部成立，但**第 14 章的三处估算基于与实现不符的假设**，当前代码存在三个隐藏放大器（A1/A2/A3）。修复后单机全天场景 Storage 行消耗可再降约 97%，面板 DO 事件可降约 50%。行号均指当前 `src/index.js`。

### 16.1 A 类：文档假设与实现不符的隐藏放大器（必须修复）

#### A1. 热区 `storage.put` 未按分钟去重 — 快采写行是估算的 9~20 倍 ✅ 已实施（2026-08-05）：`putMin` Map 记录每机已持久化分钟，仅分钟切换时 put（put 失败不置位，下帧重试，保持「写失败降级仅内存」语义）；快采 28,800 → 1,440 写/天/机（−95%），`/drop` 同步清理

- 位置：`1857`（`await this.state.storage.put(this.hotKey(...))` 每帧无条件执行）。
- 实测：快采 3s 下同分钟同 key 覆写约 20 次 → **28,800 写行/天/机**（+水位/alarm ≈29,500），免费写档 10 万行的 29%/机；3 台快采机即占 86%。慢采 720 帧/天低于原估算，无问题。
- 修法（约 5 行）：实例内存记 `serverId→minTs`，仅分钟切换时 put；put 失败置标志下帧重试（保持“写失败降级仅内存”语义，防该分钟行缺失）。
- 语义：分钟粒度一样本语义不变——正常归档读内存 Map（`archiveIncrement` 走 `m.get`），storage 行仅是 evict/fullSweep 兜底，取分钟内任一帧均为合法分钟聚合。
- 预期：快采 **28,800 → 1,440 写行/天/机（−95%）**。

#### A2. 归档兜底 `listStorage` 实际每次分钟跨线都执行 — 最大隐藏读项 ✅ 已实施（2026-08-05）：兜底条件由 `!hotLoaded` 改为「本实例内该机首帧热写」（`fresh = !data.has(serverId)`，随 `/report` 传入 `archiveIncrement`）——之后内存 Map 已覆盖本实例写入区间，滞后行由区间循环或 fullSweep（A3 修复后每 ~1h 可靠）兜底；~1.04M → 数百~数千读行/天/机，兜底 list 仅 evict 后首帧跨线执行一次

- 位置：`1812` 条件 `!this.hotLoaded.has(serverId)`；`hotLoaded` 仅在 `loadHot`（`1753`）置位，唯一调用链是 `/query`（`1877→1759`）。
- 实测：**用户不打开该机的历史图表时恒真** → 每次分钟跨线（1,440 次/天/机）都全量 `listStorage('m:{sid}:')`（≤720 行）→ **≈1.04M 读行/天/机**（5M 读档的 20%/机）。`1808` 行注释“正常运行时 hotLoaded 已置位→零额外读”的前提不成立。
- 修法（约 5 行）：兜底本意是“evict 后首帧跨线执行一次”。在 `1847-1848` 建空 Map 前记 `fresh = !this.data.has(serverId)` 传入，条件改为 `maxArchived > arcTs && fresh`（或新增 `arcPrimed` Set，`/drop` `1924-1927` 同步清除）。本实例生命周期内内存 Map 必然覆盖 `(arcTs, cutoff]`，与原意图正确性等价。
- 预期：每实例每机 ≤1 次兜底 list（仅 evict 触发），**~1.04M → 数百~数千读行/天**；顺带消除每分钟 1 行重复 `INSERT OR IGNORE`。

#### A3. `sweepCount`/`lastPrune` 为实例内存，慢采/空闲下 fullSweep 停摆 + prune 每天 144 次全表扫（额度+正确性双重问题） ✅ 已实施（2026-08-05）：`{lastSweepAt, lastPruneAt}` 持久化到 storage `housekeep` 键（`ensureHousekeepLoaded` 惰性恢复，alarm 按时间差判定、仅变化时写）；新增迁移 `0003_add_metrics_custom_ts_index.sql`（`idx_metrics_custom_ts`，schema.sql 同步）——prune 严格 1 次/天、fullSweep 恢复 ≈24 次/天，消除最坏 6.2M×S×C/天读尖峰，慢采/空闲下 12h 清理与兜底归档恢复健壮

- 位置：`1689`（`lastPrune=0`）、`1696`（`sweepCount=0`）、`2140-2143`（取模判定）、`2148-2158`（prune）。
- 实测：MetricsDO 无 WS，alarm 间隔 10 分钟，慢采/空闲时实例在两次 alarm 间必被 evict → 每次 alarm 都是新实例：
  - `sweepCount` 永远停在 1 → `1 % 6 !== 0` → **fullSweep 永不执行**：>12h 热区行不被删除 → storage 无限增长 → 反向持续放大 A2 的 list 行数（恶性循环）；兜底归档职能也停摆。
  - `lastPrune=0` → `now - 0 > PRUNE_INTERVAL_MS` 恒真 → **每次 alarm 都 prune（144 次/天）**；且 `metrics_custom` 无 ts 索引（`schema.sql:59-65` 仅 PK `(server_id, name, ts)`）→ `DELETE ... WHERE ts<?` 全表扫 43,200×S×C 行 × 144 ≈ **6.2M×S×C 读行/天**（S=1、C=1 即超 5M 读档）。
  - 注：快采 3s 时实例持续活跃，`sweepCount` 正常累积，fullSweep 可执行——此问题专属于慢采/空闲场景。
- 修法（约 15 行 + 1 个迁移）：
  1. `{lastSweepAt, lastPruneAt}` 持久化到 storage（可与 `usage:total` 合并为同一行，每次 alarm 1 读、仅变化时 1 写）；按 `now - lastSweepAt >= 6*ARCHIVE_INTERVAL_MS` 时间差判定替代内存计数。
  2. 新增迁移 `CREATE INDEX IF NOT EXISTS idx_metrics_custom_ts ON metrics_custom(ts);`（对照 `metrics_min` 已有 `idx_metrics_min_ts`，`schema.sql:56`）。
- 预期：prune 严格 1 次/天、fullSweep 恢复 ≈24 次/天；消除最坏 6.2M×S×C/天读尖峰；慢采/空闲场景 12h 清理与兜底归档恢复健壮。索引容量约 +16B/行可忽略，零语义变化。

### 16.2 B 类：其余确定的降额点（按收益排序）

| # | 位置 | 当前 | 修法 | 预期节省 |
|---|---|---|---|---|
| B1 ✅ | `2255` vs `public/app.js:300-302` | listCache TTL=3000ms 与前端 setInterval(3000) 错位，单观看者几乎 100% miss → `28,800×S` D1 读/天/观看者 | TTL→4000~5000ms | S=5 时 144,000 → ≤72,000 读/天（−50~100%） |
| B2 ✅ | `2344` | `/latest` 无缓存，每次 sync 1 DO 事件 → 28,800×V 事件/天 | PanelDO 加 3.5~5s 共享缓存（多观看者共享；卡片新鲜度最坏 3s→~6s，15s 在线宽限不受影响；告警链路用 MetricsDO 内 `lastSeenSec`，不受影响） | V=1 时 sync 链事件 −50%（57,600→~28,800+缓存 miss）；V≥2 收益线性 |
| B3 ✅ | `314-319`（调用点 `1985/2033/2136`） | SETTINGS_CACHE TTL 60s < 慢采间隔 120s → 慢采每帧上报必 miss，+720 D1 读/天/机（上报链读行翻倍）；alarm 每 10min 也必 miss | TTL→300s + `PUT /api/settings`（`957`）时对 MetricsDO 发清缓存 RPC | 慢采上报链 D1 读 −50%（−720 行/天/机） |
| B4 ✅ | `1577-1584` | PAT 终端每条浏览器消息都 `authUserByToken`（1 D1 读/消息；打字 2~5 消息/s） | 会话内存 `sess.lastPatCheck` 10s 节流 | 该路径 −98%（~300 行/min → 6 行/min）；JWT 用户本就 0 |
| B5 ✅ | `2125-2134` | usage 汇总每次 alarm 1 get + 1 put（全零也写）= 288 storage 操作/天 | 计数全零跳过 put；与 A3 家政行合并读写 | ~240 storage 操作/天（−83%） |
| B6 ✅ | `2307` 起 | sync 无消息类型校验、无频率下限：任意消息任意速率触发全链路（含 `/latest` DO 事件 + 缓存 miss 读） | 仅响应 `'sync'`；每连接记 `lastSyncAt`，<2s 直接忽略 | 防护性（正常路径 0 影响，消除无上限放大） |
| B7 ✅ | `2160-2164` | alarm 无条件 10min 续排：零服务器/闲置面板仍 144 事件 + 288 storage 行 + 144 D1 读/天 | 无数据且告警关闭时退避 +1h（新上报经 `scheduleArchive` 提前拉回） | 闲置面板 −100%；常态微收益 |
| B8（可选） ✅ | `416` | `handleReport` 每帧 `SELECT servers` 1 行（第 14 章已接受） | TerminalDO 模块级 `Map<serverId, {info_json, probe_json, name, ts}>` TTL 60s；`/rpc/drop_server`（`1168-1192`）内同步失效；写回后自更新 | 快采 −28,800 D1 读/天/机（多机留余量） |
| B9（可选） ✅ | `1543-1558→467-484` | 上报链 MetricsDO fetch 1:1 计费（28,800 事件/天/机） | TerminalDO 记 `lastFwd[serverId]`，仅 minTs 变化或 ≥10s 才转发 MetricsDO | 快采 28,800 → ~8,640 事件/天/机（−70%）；代价：卡片新鲜度 3s→≤10s，`last_seen_s` 最旧 ~13s 贴 15s 宽限边缘，需接受余量变薄 |
| B10（可选） ✅ | `2307-2368` | Panel sync 3s 轮询模型 | MetricsDO 上报驱动聚合推送（PanelDO 0→1/1→0 通知 `set_push`；`/report` 处理时 ≥3s 节流推全部 latestByServer → PanelDO `/rpc/latest_push` per-ws 权限过滤广播；**不引入定时器，MetricsDO 休眠不受影响**——修正原"每 3s 主动推"会破坏 Hibernation 的描述）；前端连接时发一次 sync 拉初始、此后被动收推送，`pushTimer` 改 1s 本地在线老化（last_seen_s 倒计时 20s 判离线） | 成本从 `28,800×V` 轮询 → **28,800 封顶（与 V、S 解耦）**；V=1 省 ~50%、V≥2 线性放大 |

### 16.3 核对后“无需改”的项

- **audit 写入**（`767/791/850/870`）：全部用户动作驱动，≪100 行/天，无优化空间。
- **prune 间隔本身**：24h 合理；问题只在 evict 重置（A3），不是频率。降频不省写行（总量由保留期决定）。
- **schema 其余**：`idx_metrics_min_ts`（`56`）必须保留；`audit_logs` 量小无需索引；D1 按行不按字节计费，`SELECT *` 行宽只影响 CPU 不影响额度。
- **TerminalDO 心跳 ping**（`1559-1566`，30s 限频）：出站消息不计费。
- **`/api/usage` 端点自身**：1 DO 事件 + 1 storage 读/次，管理员低频调用，可忽略（其 `estimates_per_day` 把累计值标注为 per_day，属观测准确性瑕疵，非额度问题，建议后续改注释或按近 24h 窗口统计）。
- **handleReport 的 1 行 SELECT（`416`）**：已被第 14 章接受，仅作可选 B8。

### 16.4 修复 A+B 后的总量对比（1 机快采 + 1 可见观看者全天）

| 资源 | 文档 14.2 原估算 | 实测当前 | A+B 修复后 |
|---|---:|---:|---:|
| DO 事件/天 | 115,344 | ~115,344 | **~43,000~58,000**（B2 生效；B9 再 −20,000） |
| DO Storage 读/天 | 17,400 | **≤~1.05M**（无人看图谱时） | **~20,000** |
| DO Storage 写/天 | 3,100 | **~29,500** | **~3,200** |
| D1 读/天 | 29,000~39,000 | ~44,000+5,760×S + 最坏 6.2M×C prune 尖峰 | **~3,000~5,000** |
| D1 写/天 | 10,000~13,000 | 同左 | 同左（不变） |

### 16.5 结论与优先级

1. **A1/A2/A3 各改约 5~15 行，合计把 Storage 行消耗降约 97%**；A3 同时是正确性修复（慢采/空闲下 12h 清理停摆 → 热区无限增长），建议最先做。
2. **B1/B2 让面板侧 DO 事件降约 50%**：1 机 1 观看者全天从 11.5 万事件回落到免费档内（~5.8 万）；B1 顺带把单观看者 D1 读减半。
3. B3~B7 为小额确定收益，可同批实施；B8~B10 为可选项，仅在“5 机全天大屏留在免费档”或 PAT 终端高频使用场景下需要。
4. 全部修复不改变鉴权边界、权限过滤、原子写、幂等重试、数据保留语义与 Agent 协议；A 类与 B1/B2/B3 建议补回归测试（分钟去重 put、兜底仅首帧、持久化家政状态跨 evict、缓存 TTL 命中）。

### 16.6 验证后跟进修复（2026-08-05，独立复核 + 逐行核实）

实施全部落地后经独立验证发现 4 个问题，已修复（`930b8c1` 之后）：

| # | 问题 | 严重度 | 修法 |
|---|---|---|---|
| 1 | **B5 全零跳过是死代码**：`if (report \|\| latest \|\| query)` 用累计值（prev+本期）判断，历史有过流量即永远非零 → put 永远执行 | 低 | 改判本期增量 `this.usage.report/latest/query`（一行） |
| 2 | **B10 撤销后观看者连接不关闭**：`latest_push` 对 `!user` 仅 return，而 B10 后前端只发一次 sync → `webSocketMessage` 关闭分支永不触发 → 撤销连接残留 `/viewers` 计数维持快采 | 中低 | `latest_push` 中 `!user` 时 `w.close(1008)`（连接级兜底） |
| 3 | **pushOn 无自愈**：MetricsDO 无 WS 易 evict，`pushOn` 内存态丢失后不再推送 → 面板冻结、20s 老化全离线，需刷新才恢复 | 低 | pushOn=false 且有上报时 30s 低频反查 `/viewers`，有人看则恢复 |
| 4 | **A2 极窄回退**：首帧兜底 list 失败后 `fresh=false` 永不重试，后续帧正常推进水位会越过 storage-only 滞后行（需时钟回拨+evict+瞬时失败组合） | 极低 | `pendingArcRetry` Set：失败置标记、后续帧继续重试兜底、成功清除、`/drop` 清理 |

新增回归测试 4 项（撤销关闭、pushOn 自愈、pending 重试、B5 本期增量）。
验证：JS 119/119、E2E 9/9。

### 16.7 多机批量上报（记录于 2026-08-05，随后实施）

**建议来源**：独立分析提出「MetricsDO /report 批量接口」——TerminalDO 分片内把同一节流窗口（B9 的 5s）内多台机器的上报聚合成一次 fetch。

- 定位：与 B9（时间节流）/B10（下游聚合）同方向、不同维度——**空间批量**（多机同分片合并 fetch）
- 收益：5 机分布 4 分片时 fetch 36,000 → 28,800（−20%）；同分片多机时收益更大（−50%）；单机无收益（B9 已单机节流到 5s/帧）
- 改动：`/report` 支持批量 body（frames 数组）+ MetricsDO 热写/告警/归档循环批量化 + TerminalDO/Worker 侧 5s 窗口攒批触发式 flush + 测试
- 实现要点：批量 flush 沿用**上报驱动触发**（每帧到达检查距上次 flush ≥5s 才 flush），不引入定时器（TerminalDO 休眠不受影响）；`reportFwd` 单机节流升级为 `reportBatch` 队列 + flush
- 实施（2026-08-05）：`handleReport` 转发改为 `reportBatch` 队列 + 5s 触发式 flush（`{frames:[...]}` 批量 body）；MetricsDO `/report` 支持批量并抽 `processReportFrame`（热写/告警/归档/推送逐帧复用，B10 推送按 5s 节流）；单帧格式兼容；同机 5s 窗口内保留最新帧
- 状态：✅ 已实施；验证：JS 121/121、E2E 9/9
