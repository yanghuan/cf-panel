{
  "name": "cf-panel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare 上的带终端功能的监控面板（Worker + Durable Objects + D1）",
  "scripts": {
    "test": "node --test",
    "test:unit": "node --test test/unit.test.js",
    "test:api": "node --test test/api.test.js",
    "test:do": "node --test test/do.test.js",
    "test:e2e": "bash test/e2e.sh",
    "migrate": "wrangler d1 migrations apply cf-panel --remote",
    "migrate:local": "wrangler d1 migrations apply cf-panel --local",
    "deploy": "wrangler d1 migrations apply cf-panel --remote && wrangler deploy",
    "deploy:local": "wrangler d1 migrations apply cf-panel --local && wrangler dev --local"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
